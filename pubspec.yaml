name: networking_plus
description: A Flutter Library for calling rest APIs very easily and get rid of a lots of boiler plate code base.
version: 0.0.1
homepage: https://github.com/rehmnabdul/networking-plus

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter: